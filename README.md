Built using React as part of FreeCodeCamp's Data Visualization Certification curriculum.

To install dependencies, run 'npm install', then use 'npm run dev' to start server. Access from localhost:8080.